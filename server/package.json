{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "openai": "^4.68.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
