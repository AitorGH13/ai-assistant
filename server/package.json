{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "uvicorn main:app --reload --port 3001",
    "dev:voice": "bun run index.ts",
    "start": "uvicorn main:app --port 3001",
    "start:voice": "bun run index.ts",
    "install": "pip install -r requirements.txt"
  },
  "dependencies": {
    "@types/node": "^25.2.3",
    "elevenlabs-js": "^1.2.6"
  },
  "devDependencies": {
    "bun-types": "^1.3.9"
  }
}
